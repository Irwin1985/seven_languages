(ns project.core
  (:gen-class))

(defn big "Robert's first clojure function" [st n] (
  (println "TODO")
))


(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Start of Day 1"))
