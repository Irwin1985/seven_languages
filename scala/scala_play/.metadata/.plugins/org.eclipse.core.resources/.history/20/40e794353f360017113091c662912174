object aaa {
  println("Welcome to the Scala worksheet")
  println("TDSFFDS")
  var aaa = List("ee","fdfdsf","gfdr")
  for ( x <- aaa) {  println(x) }
  
  for (i <- List(1,2,3, 4)) yield i * 2
  
  var myList = List(11,2,4,65,32,2,3)
  var sum = myList.foldLeft(0)((sum, value) => sum + value)
  myList.head
  
  var max = myList.foldLeft(myList.head)((max, value) => {
  	if (value > max) value
  	else max
  }
  )

  var min = myList.foldLeft(myList.head)((min, value) => {
  	if (value < min) value
  	else min
  }
  )
  
  for (i <- Lisrt
  
}